name: flutter_plugin_tuts
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_info: ^0.4.2+6
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
